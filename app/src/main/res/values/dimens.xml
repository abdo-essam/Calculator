<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Button Dimensions -->
    <dimen name="button_size">@dimen/_50sdp</dimen>
    <dimen name="button_margin">@dimen/_8sdp</dimen>

    <!-- Button Text Size -->
    <dimen name="button_text_size">@dimen/_18ssp</dimen>

    <!-- Container Margins -->
    <dimen name="container_margin_horizontal">0dp</dimen>
    <dimen name="container_margin_vertical">@dimen/_24sdp</dimen>
    <dimen name="container_margin_top">@dimen/_12sdp</dimen>

    <!-- Row Margins -->
    <dimen name="row_margin_bottom">@dimen/_8sdp</dimen>

    <!-- Text Sizes -->
    <dimen name="text_size_expression">@dimen/_16ssp</dimen>
    <dimen name="text_size_result">@dimen/_42ssp</dimen>

    <!-- Text Paddings -->
    <dimen name="text_padding_horizontal">@dimen/_20sdp</dimen>
    <dimen name="text_padding_bottom">@dimen/_24sdp</dimen>

    <!-- Bottom Indicator -->
    <dimen name="indicator_width">@dimen/_28sdp</dimen>
    <dimen name="indicator_height">@dimen/_3sdp</dimen>
    <dimen name="indicator_margin_bottom">@dimen/_6sdp</dimen>

    <!-- Icon Size -->
    <dimen name="icon_size">@dimen/_14sdp</dimen>
</resources>